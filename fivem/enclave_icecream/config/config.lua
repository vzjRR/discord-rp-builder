---@diagnostic disable: lowercase-global
--[[
    enclave_icecream — الإعدادات العامة
    ------------------------------------
    كل الأرقام والمفاتيح هنا. ما تحتاج تلمس أي ملف داخل client/ أو server/.
    المواقع في config/locations.lua — الوصفات في config/recipes.lua
]]

Config = {}

-- ────────────────────────────────────────────────────────────
-- 1) اللغة
-- ────────────────────────────────────────────────────────────
-- يجب أن يوجد ملف بنفس الاسم داخل locales/  (ar.json / en.json)
Config.Locale = 'ar'

-- ────────────────────────────────────────────────────────────
-- 2) التكامل مع الفريمويرك والموارد
-- ────────────────────────────────────────────────────────────
-- 'auto' يكتشف الموجود على السيرفر تلقائيًا. غيّرها فقط لو الاكتشاف فشل.
Config.Framework = 'auto' -- 'auto' | 'qbx' | 'qb' | 'esx' | 'standalone'
Config.Inventory = 'auto' -- 'auto' | 'ox' | 'qb' | 'esx' | 'none'
Config.Target    = 'auto' -- 'auto' | 'ox' | 'qb'

-- استخدام قاعدة البيانات (oxmysql). لو false: حساب الشركة والإحصائيات
-- تُحفظ في الذاكرة فقط وتضيع عند إعادة تشغيل السيرفر.
Config.UseDatabase = true

-- ────────────────────────────────────────────────────────────
-- 3) الوظيفة
-- ────────────────────────────────────────────────────────────
Config.Job = {
    name = 'icecream',        -- يجب أن يطابق اسم الوظيفة في الفريمويرك
    label = 'محل المثلجات',

    -- الرتب: المفتاح هو رقم الرتبة (grade) في الفريمويرك
    grades = {
        [0] = { label = 'متدرب',       canCraft = true,  canRegister = false, canFreezer = true,  canSupply = false, isBoss = false },
        [1] = { label = 'موزّع',        canCraft = true,  canRegister = true,  canFreezer = true,  canSupply = false, isBoss = false },
        [2] = { label = 'صانع مثلجات',  canCraft = true,  canRegister = true,  canFreezer = true,  canSupply = true,  isBoss = false },
        [3] = { label = 'مساعد مدير',   canCraft = true,  canRegister = true,  canFreezer = true,  canSupply = true,  isBoss = false },
        [4] = { label = 'المالك',       canCraft = true,  canRegister = true,  canFreezer = true,  canSupply = true,  isBoss = true  },
    },

    -- الرتبة الافتراضية عند التوظيف من منيو البوس
    hireGrade = 0,
    -- أعلى رتبة يقدر البوس يرقّي لها (يمنع ترقية أحد لمالك)
    maxPromoteGrade = 3,
}

-- إجبار تسجيل الدوام قبل أي عمل. لو false، كل الميزات شغالة لأي موظف.
Config.RequireDuty = true

-- منع اللاعب من الخروج من الدوام وهو يحمل منتجات (يجبره يرجّعها للفريزر)
Config.BlockOffDutyWithStock = false

-- ────────────────────────────────────────────────────────────
-- 4) الاقتصاد
-- ────────────────────────────────────────────────────────────
Config.Economy = {
    -- نوع الفلوس الشخصية عند الدفع/القبض
    playerAccount = 'bank',  -- 'cash' | 'bank'
    -- نوع الفلوس اللي يدفع فيها الزبون اللاعب عند الفوترة
    billAccount   = 'bank',

    -- توزيع قيمة بيع الطلب: كم نسبة تروح للشركة وكم بقشيش للموظف
    societyShare  = 0.75,   -- 75% للشركة
    employeeTip   = 0.25,   -- 25% بقشيش مباشر للموظف (المجموع يجب أن يساوي 1.0)

    -- سقف البقشيش لكل طلب (حماية من الاستغلال)
    maxTipPerOrder = 250,

    -- رصيد افتتاحي لحساب الشركة عند أول تشغيل
    startingBalance = 25000,

    -- السماح لرصيد الشركة أن يصبح سالبًا (لا يُنصح)
    allowNegativeBalance = false,
}

-- الرواتب الدورية (تُخصم من حساب الشركة وتُضاف للموظف المتواجد وعلى الدوام)
Config.Paycheck = {
    enabled = true,
    intervalMinutes = 30,
    -- المبلغ لكل رتبة
    amounts = { [0] = 250, [1] = 400, [2] = 600, [3] = 850, [4] = 1200 },
    -- لا يُدفع الراتب إذا رصيد الشركة أقل من هذا
    minBalance = 0,
    requireDuty = true,
}

-- ────────────────────────────────────────────────────────────
-- 5) التصنيع
-- ────────────────────────────────────────────────────────────
Config.Crafting = {
    -- مضاعف عام لأوقات التصنيع (0.5 = أسرع بمرتين)
    timeMultiplier = 1.0,
    -- اختبار مهارة ox_lib أثناء التصنيع
    skillCheck = {
        enabled = true,
        -- الوصفات ذات difficulty أعلى من هذا فقط تطلب اختبار مهارة
        minDifficulty = 2,
    },
    -- إلغاء التصنيع إذا ابتعد اللاعب عن المحطة
    maxDistance = 3.0,
    -- الحد الأقصى لعدد القطع في الدفعة الواحدة
    maxBatch = 10,
    -- أقل فاصل زمني بين عمليتي تصنيع لنفس اللاعب (ملي ثانية) — حماية سيرفرية
    cooldownMs = 750,
}

-- ────────────────────────────────────────────────────────────
-- 6) طلبات الزبائن (NPC)
-- ────────────────────────────────────────────────────────────
Config.Orders = {
    enabled = true,
    -- أقصى عدد طلبات معلّقة في نفس الوقت لكل فرع
    maxPending = 5,
    -- كل كم ثانية يُولَّد طلب جديد (يُختار رقم عشوائي بين الحدين)
    spawnDelay = { min = 45, max = 120 },
    -- مدة صلاحية الطلب بالثواني قبل ما يمشي الزبون
    expiry = 300,
    -- لا تُولّد طلبات إلا إذا كان في هذا العدد من الموظفين على الدوام
    minEmployeesOnDuty = 1,
    -- مضاعف السعر حسب الوقت والطقس (يشجع اللعب النهاري الواقعي لمحل مثلجات)
    multipliers = {
        dayTime  = 1.25,  -- من 10 صباحًا إلى 8 مساءً
        hotWeather = 1.35, -- EXTRASUNNY / CLEAR
        night    = 0.85,
    },
    -- عقوبة انتهاء صلاحية الطلب على حساب الشركة (0 = بلا عقوبة)
    expirePenalty = 0,
}

-- ────────────────────────────────────────────────────────────
-- 7) الفوترة اليدوية (بيع للاعبين)
-- ────────────────────────────────────────────────────────────
Config.Register = {
    enabled = true,
    -- أقصى مبلغ في الفاتورة الواحدة
    maxAmount = 5000,
    -- أقصى مسافة بين الكاشير واللاعب المستهدف
    maxDistance = 5.0,
    -- مدة انتظار موافقة اللاعب بالثواني
    acceptTimeout = 30,
}

-- ────────────────────────────────────────────────────────────
-- 8) التوريد
-- ────────────────────────────────────────────────────────────
Config.Supply = {
    enabled = true,
    -- مصدر الدفع لشراء المواد الخام
    payFrom = 'society',  -- 'society' | 'player'
    -- أقصى كمية في عملية شراء واحدة
    maxQuantity = 100,
    -- قائمة المواد الخام وأسعارها للوحدة (اسم العنصر = السعر)
    catalog = {
        { item = 'ic_milk',       label = 'حليب',            price = 12,  max = 100 },
        { item = 'ic_cream',      label = 'قشطة',            price = 18,  max = 100 },
        { item = 'ic_sugar',      label = 'سكر',             price = 6,   max = 200 },
        { item = 'ic_cocoa',      label = 'كاكاو',           price = 22,  max = 60  },
        { item = 'ic_vanilla',    label = 'فانيليا',          price = 25,  max = 60  },
        { item = 'ic_strawberry', label = 'فراولة',          price = 20,  max = 60  },
        { item = 'ic_mango',      label = 'مانجو',           price = 20,  max = 60  },
        { item = 'ic_pistachio',  label = 'فستق',            price = 30,  max = 40  },
        { item = 'ic_flour',      label = 'طحين',            price = 8,   max = 100 },
        { item = 'ic_cup',        label = 'كوب ورقي',        price = 3,   max = 200 },
        { item = 'ic_topping',    label = 'توبينغ متنوع',     price = 10,  max = 100 },
    },

    -- جولة التوريد بالشاحنة (اختيارية — ربح إضافي)
    run = {
        enabled = true,
        vehicle = 'boxville2',
        payout = { min = 900, max = 1600 },
        -- عدد نقاط التحميل المطلوبة قبل التسليم
        pickups = 3,
        cooldownMinutes = 10,
    },
}

-- ────────────────────────────────────────────────────────────
-- 9) عربة المثلجات المتنقلة
-- ────────────────────────────────────────────────────────────
Config.Truck = {
    enabled = true,
    model = 'taco',                  -- موديل العربة (taco = Taco Van، بديل واقعي متوفر بالأساس)
    rentalFee = 500,                 -- تُخصم من حساب الشركة عند الإخراج
    requireGrade = 1,
    -- مضاعف سعر البيع المتنقل مقابل المخاطرة والوقت
    priceMultiplier = 1.4,
    -- المدة بالثواني بين بيعة وأخرى في نفس النقطة
    saleCooldown = 20,
    -- أقصى مسافة من العربة للبيع
    maxDistance = 8.0,
}

-- ────────────────────────────────────────────────────────────
-- 10) ذوبان المثلجات (خاصية مميزة لمحل المثلجات)
-- ────────────────────────────────────────────────────────────
Config.Melting = {
    enabled = true,
    -- كل كم ثانية تُفحص نضارة المنتجات في يد اللاعب
    checkInterval = 60,
    -- بعد كم دقيقة يبدأ المنتج يفقد قيمته خارج الفريزر
    freshMinutes = 15,
    -- أقل نسبة من السعر يوصلها المنتج الذائب تمامًا
    minValueRatio = 0.35,
    -- بعد كم دقيقة يصير المنتج غير صالح للبيع نهائيًا (0 = لا يتلف)
    ruinMinutes = 45,
}

-- ────────────────────────────────────────────────────────────
-- 11) الإشعارات والواجهة
-- ────────────────────────────────────────────────────────────
Config.UI = {
    -- 'ox' يستخدم lib.notify. غيّرها لـ 'framework' لاستخدام إشعارات QB/ESX
    notify = 'ox',
    contextPosition = 'top-right',
    -- عرض البلبس على الخريطة
    blips = true,
    -- عرض TextUI عند الاقتراب من نقاط الدوام
    textUI = true,
}

-- ────────────────────────────────────────────────────────────
-- 12) إعدادات السيرفر (اللوقات والويبهوك)
-- ────────────────────────────────────────────────────────────
-- ⚠️ هذا الملف shared — أي شيء تكتبه هنا ينزل لجهاز اللاعب ويقدر يقرأه.
--    لذلك إعدادات اللوقات ورابط الويبهوك في ملف سيرفري بحت:
--        server/config.server.lua
--    لا تضع أي سرّ في هذا الملف.

-- ────────────────────────────────────────────────────────────
-- 13) تصحيح الأخطاء
-- ────────────────────────────────────────────────────────────
Config.Debug = false          -- يطبع تفاصيل إضافية في الكونسول
Config.DebugZones = false     -- يرسم مناطق ox_target
